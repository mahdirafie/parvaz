import 'package:flutter/material.dart';


class AcceptedCard extends StatelessWidget {
  const AcceptedCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    return InkWell(

    );
  }
}
